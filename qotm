#!/bin/bash

# Fetch a random quote and display it...

# Path to Js Source file
path=./quotes/fetch_quote.js    # Change this path to an absolute path if you want to run from bin

# Check if both cowsay and lolcat packages are installed, then use them to format the output
if [ -x /usr/games/cowsay -a -x /usr/games/lolcat ]; then
    node $path | cowsay | lolcat
else
    if [ -x /usr/games/cowsay ]; then
        node $path | cowsay
    else
         node $path
    fi
fi
